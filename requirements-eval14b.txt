# Extra deps for evaluate_with_qwen14b.py (Qwen2.5-14B as evaluator)
# Base: transformers, torch (from LLaMA-Factory / venv)
transformers>=4.37.0
torch
# 4-bit / 8-bit (transformers backend):
bitsandbytes

# Optional, Apple Silicon only (--backend mlx):
# mlx
# mlx-lm
