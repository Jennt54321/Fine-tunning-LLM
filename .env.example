# Copy to .env and fill in as needed. Never commit .env.

# HuggingFace: for gated models and higher rate limits
# HF_TOKEN=

# evaluate_with_qwen14b.py defaults (optional overrides)
# EVAL_MODEL_ID=Qwen/Qwen2.5-14B-Instruct
# EVAL_QUANTIZE=4bit
# EVAL_BACKEND=transformers
